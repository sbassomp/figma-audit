"""Static assets and Jinja2 templates for the web dashboard.

This package contains no code — it only exists so setuptools includes the
``templates/*.html`` and ``static/*`` files as package data when building
the wheel.
"""
